## manifest/Makefile.am
## The manifest library sources are compiled directly into the cmake
## studio and runtime builds (not separately by autotools).
## This Makefile.am exists to satisfy SUBDIRS and provide EXTRA_DIST.

AUTOMAKE_OPTIONS = foreign

EXTRA_DIST = \
    Manifest.h \
    Manifest.cpp

# No targets — sources included by cmake builds above.
